# Domaestro Configuration

# Required: Authentication token for API access
# Generate a secure random token for production use
AUTH_TOKEN=your-secret-token-here

# Optional: Comma-separated list of valid CNAME targets for domain validation
# These are the domains that registered domains can point to via CNAME
VALID_CNAMES=example.com,api.example.com

# Optional: Comma-separated list of valid IP addresses for domain validation
# These are the IP addresses that registered domains can point to via A record
VALID_IPS=192.168.1.1,10.0.0.1

# Example production configuration:
# AUTH_TOKEN=super-secure-random-token-12345
# VALID_CNAMES=yourdomain.com,api.yourdomain.com
# VALID_IPS=1.2.3.4,5.6.7.8
